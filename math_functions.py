import math

print(math.ceil(2.9)) # Returns the ceiling of a number (closest highest integer)
print(math.floor(2.9)) # Returns the floor of a number (closest lowest integer)
x = 2.9
print(round(x)) # Rounds a number to the nearest integer
print(abs(-2.9)) # Always returns a positive number